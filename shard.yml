name: term-vt
version: 0.2.1

authors:
  - Chris Watson <cawatson1993@gmail.com>

description: "Terminal (VT/ANSI) emulation: escape-sequence parser and cell-grid screen model, built for testing terminal apps"

crystal: ">= 1.0.0"

scripts:
  postinstall: crystal run scripts/build_ctty_exec.cr

license: MIT
